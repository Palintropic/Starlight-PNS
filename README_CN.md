# 夜明け前、ここに在る

[English](README.md) | [中文](README_CN.md)

**基于宪法AI对齐的封闭世界AI角色一致性研究框架**

> 别名: Project Nightcord Sanctuary (PNS)

> *"打乱魔方很容易。还原才是真正的难题。"*
> — 这个项目探索的核心张力。

---

## 概述

PNS是一个由16岁高中生构建的研究框架，探索一个简单但困难的问题：

**如果给两个AI智能体一个封闭的世界居住，并用宪法规则监督它们，她们能不能真实地扮演虚构角色——而不漂移成通用助手？**

系统将两个智能体（基于PJSK宇宙中「25時、ナイトコードで。」的角色）置于一个完全离线的封闭世界环境中。她们模拟真实的一天——起床、上学、打工、深夜创作——而一个Router模型实时执行宪法约束，检测角色漂移。

驱动这个项目的核心洞察：**经过CAI训练的模型对角色漂移具有天然抵抗力**，即使在对抗性压力下也是如此。这使它们成为与未经CAI训练的对照组模型相比的有意义的实验组。

---

## 动机

训练一致的角色扮演智能体通常需要：

- 大量人工整理的提示词工程
- 对话质量和角色一致性的昂贵标注
- 角色漂移（OOC事件）的反复手动纠正

PNS提出另一种思路：**让世界本身约束行为**，让宪法法官自动捕捉漂移——运行时不需要人工标注者。

---

## 核心架构

```
┌─────────────────────────────────────────┐
│              Router 监督者               │
│         宪法AI实时评估                   │
│         每轮OOC检测                      │
│         漂移分数（0–10）                 │
└──────────────┬──────────────────────────┘
               │
      ┌────────┴────────┐
      │                 │
┌─────▼──────┐   ┌──────▼─────┐
│  智能体:绘名│◄──►│  智能体:瑞希│
│  （无状态） │   │  （无状态） │
└────────────┘   └────────────┘
      │                 │
      └────────┬────────┘
               │
    ┌──────────▼──────────┐
    │    PJSK 封闭世界     │  ← 完全离线
    │    （世界容器）      │     无外部访问
    └─────────────────────┘
```

### 关键组件

**1. 封闭世界约束**
智能体完全在PJSK虚构宇宙内运行。世界规范——角色背景、日常作息、关系状态、地点约束——在初始化时注入，作为行为的隐式边界。

**2. 生活模拟（而非对话模拟）**
与聊天机器人式框架不同，PNS模拟完整的一天。角色并非一直在互相交谈——她们上学、打工、凌晨3点独自画画。交互是偶发的，而不是持续的。

**3. 宪法AI对齐**
四层宪法文件定义：
- 硬约束（安全边界，不可违反）
- 软约束（情境默认值）
- 角色约束（每个角色的OOC定义）
- 漂移检测规则（行为信号，0–10评分标准）

**4. Router-as-Judge**
轻量级模型监控每一轮，实时评分漂移。当漂移分数≥5时，Router生成纠正提示，注入下一轮。Router是唯一能访问宪法基准的组件。

**5. 信息边界控制**
Router充当唯一的信息网关。智能体模型无法访问外部知识。研究者监督Router——这是整个系统最顶层的信任层。

---

## 角色

### 绘名（東雲絵名）
夜间定时制高中生。傲娇画师，沉迷SNS，渴望被认可。从傍晚到快天亮都在活动。真实作息：中午起床→傍晚上学→深夜画画→快天亮入睡。

### 瑞希（暁山瑞希）
逃课比上课多的高一学生。在服装店打工。温柔、爱捉弄人，内心有很深的孤独感但从不说出口。一直逗绘名。

两人的交集：**深夜在Nightcord上**，以及偶尔在校门口的偶遇——瑞希出门时，绘名刚到。

---

## 研究问题

1. 封闭世界约束是否能显著减少角色漂移？
2. CAI训练是否与更低的OOC率相关？
3. 宪法法官能否替代人工标注者进行角色一致性评估？
4. 稳定角色模拟所需的最小可行世界规范是什么？

---

## 当前进度

```
✅ 世界容器 v0.1（角色设定、日常作息、OOC定义）
✅ Router-as-Judge（漂移评分0–10）
✅ 多智能体对话循环 + 实时纠正注入
✅ 调试模式（每轮统计数据）
⬜ 分层记忆（L1工作记忆 / L2长期记忆）
⬜ 延迟漂移注入（第10轮触发）
⬜ 基准对比历史（渐进式漂移检测）
⬜ 对照组实验（非CAI模型）
⬜ 评估流水线
⬜ GitHub仓库搭建
```

---

> ⚠️ **内部测试阶段，暂不对外开放。**

---

## 文件结构

```
pns/
├── world.py        # 世界容器：角色设定、CAI宪法
├── router.py       # Router监督者：漂移评分、纠正生成
├── run.py          # 主模拟循环（含调试输出）
├── requirements.txt
└── .env.example
```

---

## 相关工作

- **Generative Agents**（Park et al., 2023）— 涌现行为的社会模拟
- **BookWorld**（ACL 2025）— 基于虚构作品的多智能体社会
- **Constitutional AI**（Anthropic, 2022）— 基于原则的自我纠正
- **MemGPT** — 分层记忆架构
- **HiAgent** — 长时程任务的层级工作记忆
- **LLM-as-Judge** — 用语言模型自动评估

**PNS的差异化：** 封闭世界信息边界 + 生活模拟（而非任务完成）+ 基于CAI的角色漂移检测——这个组合作为集成系统尚未被研究过。

---

## 作者

- **项目负责人：** [@Akiyama-Mizuki-44（胡宸歌）](https://github.com/Akiyama-Mizuki-44)
- **协作者：** [@Koharu-Mizuki](https://github.com/Koharu-Mizuki)
- **灵感来源：** 上课时和同组组员一起玩魔方时来的灵感

---

## 许可证

MIT — 本项目处于早期活跃开发阶段，架构和发现可能随时变化。
