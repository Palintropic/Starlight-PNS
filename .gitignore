# ─── Python ───────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
.venv/
venv/
env/

# ─── 环境变量 ──────────────────────────────────────────────
.env
*.env.local
# 示例配置里只有变量名和占位符，它要进版本库。
!.env.example

# ─── Visual Studio ────────────────────────────────────────
.vs/
*.suo
*.user
*.userosscache
*.sln.docstates
*.vcxproj.filters
*.vcxproj.user
*.VC.db
*.VC.opendb
ipch/
x64/
x86/
[Dd]ebug/
[Rr]elease/
[Bb]in/
[Oo]bj/

# ─── VS Code ──────────────────────────────────────────────
.vscode/

# ─── macOS ────────────────────────────────────────────────
.DS_Store

# ─── 日志 / 临时 ──────────────────────────────────────────
*.log
*.tmp
session_log*.json
history/

# ─── 密钥 / 凭证 ──────────────────────────────────────────
.env
.env.*
!.env.example
secrets.json
secrets.yaml
config.secret.py
*_secret.*
*_key.*

.claude/session-notes.md

# ─── World Editor 保存前的覆盖式备份（不是历史版本链）───────
pns/world/*.py.bak

# ─── 人工审核决策（运行时数据，跟着 data/ 卷走）─────────────
data/review_decisions.jsonl
/review_decisions.jsonl

# ─── 账户库（AUTH-1 的用户与安全审计，本地运行时数据）───────
# 里面是 Argon2id 哈希和审计记录，不是源码。位置可用 PNS_ACCOUNTS_DB 改。
data/accounts.sqlite3
data/accounts.sqlite3-journal

# ─── 持久世界存档 / 所有权锁（本地运行时数据）──────────────
# 存档根默认在 data/worlds/，可用 PNS_WORLD_ROOT 改到别处。
# 它装的是一个跑着的世界的权威状态和一把进程锁，不是源码。
data/worlds/

# ─── 开工/规划文档（给 CC 用，不进仓库）────────────────────
/kickoff/
/research/