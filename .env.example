# Starlight-PNS 运行时配置示例
#
#   cp .env.example .env   然后把每一处 replace-with-… 换成真值
#
# 这份文件里**只有变量名和占位符**，一个真凭据都没有，所以它可以进版本库。
# 复制出来的 .env 不进版本库、不进构建上下文、也不进镜像（见 .dockerignore）。
# 容器里没有 .env 文件；配置是由 Compose 注入进程环境的。
#
# **任何含 `$` 的值都要用单引号包起来**（Compose 会对没引号的值做变量替换）。
#
# PNS_ENV 不在这里：它由镜像和 compose.yaml 固定为 production，好让一次
# 手滑改不动这台服务器的模式。

# ── 必填：break-glass / 自动化凭据 ──────────────────────────────────────
# 用 `openssl rand -hex 32` 生成。至少 32 个字符，不能是占位串——生产模式下
# 缺它、太短、或者还是下面这个示例值，进程都会直接起不来。
#
# 它**只走 `Authorization: Bearer`**，不是网页口令：浏览器登录用账户（下面
# 那一节）。理由是它不属于任何人、不会过期、撤销要重启进程——让它同时当网页
# 口令，账户体系里的停用/改角色/改密码就全都绕得过去。
PNS_ADMIN_TOKEN=replace-with-openssl-rand-hex-32-output

# ── 必填（生产）：第一个管理员账户 ──────────────────────────────────────
# 生产模式要求至少一个启用着的管理员，否则进程起不来。哈希这样生成：
#
#     docker compose run --rm --entrypoint "" app \
#       python scripts/accounts.py hash-password
#
# **明文密码永远不写在这里。**下面填的是那条命令打印出来的 $argon2id$ 串。
#
# **哈希必须用单引号包起来。**它里面有好几个 `$`，而 Docker Compose 会把
# env_file 里没引号（或用双引号）的值做一次变量替换，于是 `$argon2id`、`$v`、
# `$m` 这些段会被当成未定义变量换成空串——服务器会因此起不来并如实报错，但
# 那半小时的排查完全可以省掉。单引号里的内容 Compose 原样透传。
#
# 这两行可以一直留着：引导是幂等的，库里已经有账户时它什么都不做，所以它也
# 不能被用来把一个已存在账户的密码改回去。
# PNS_BOOTSTRAP_ADMIN_USERNAME=replace-with-your-username
# PNS_BOOTSTRAP_ADMIN_PASSWORD_HASH='replace-with-argon2id-hash'

# ── 必填：模型 provider ─────────────────────────────────────────────────
# 变量名要跟 PNS_API_KEY_NAME 对上。下面这组是 Xiaomi MiMo 按量付费的形状；
# 换 provider 时四行一起改（可选值见 scripts/oobe.py 的 PROVIDERS 表）。
PNS_API_KEY_NAME=MIMO_API_KEY
MIMO_API_KEY=replace-with-your-provider-api-key
API_FORMAT=anthropic
BASE_URL=https://api.xiaomimimo.com/anthropic
PROVIDER=Xiaomi MiMo API

# 生成模型与判分模型可以不同；只写 MODEL 时两者都回退到它。
MODEL=mimo-v2.5-pro
GENERATOR_MODEL=mimo-v2.5-pro
EVALUATOR_MODEL=mimo-v2.5-pro

# ── 可选：账户库位置 ────────────────────────────────────────────────────
# 默认 /app/data/accounts.sqlite3，也就是 pns-data 卷里面（0600）。改之前先
# 确认新位置也在本地文件系统的持久卷上。
# PNS_ACCOUNTS_DB=/app/data/accounts.sqlite3

# ── 可选：反向代理改写了 Host 时，浏览器实际访问的源 ────────────────────
# 写操作会检查 Origin 与 Host 同源（SameSite 之外的第二把锁）。代理如果不透传
# 原始 Host（nginx 里写 `proxy_set_header Host $host;` 就够了），把浏览器实际
# 访问的源逗号分隔写在这里。留空是更安全的默认：对不上就拒绝。
# PNS_TRUSTED_ORIGINS=https://pns.example.lan,https://192.168.3.115

# ── 可选：会话与网络 ────────────────────────────────────────────────────
# 反向代理提供 HTTPS 时把它设成 true。明文访问下设成 true 会让 Cookie 根本
# 发不出去，所以默认是 false，由操作者显式打开。
PNS_SESSION_COOKIE_SECURE=false
# 操作者登录后的会话有效期（秒）。会话只活在进程内存里，重启就得重新登录。
PNS_SESSION_TTL_SECONDS=43200
# 宿主上监听的地址与端口。默认只绑回环——TLS 和防火墙属于操作者。
PNS_BIND=127.0.0.1
PNS_PORT=7860

# ── 可选：自主运行节律 ──────────────────────────────────────────────────
# 全部是服务器侧冷配置，浏览器一个字节都传不进来。看不懂的值会让进程响亮
# 失败，不会悄悄回落到默认值。
# PNS_AUTONOMY_TICK_MINUTES=5
# PNS_AUTONOMY_INTERVAL_SECONDS=30
# PNS_AUTONOMY_ACTIVATIONS_PER_RUN=200
# PNS_AUTONOMY_WORLD_ACTION_CAP=100000
# PNS_AUTONOMY_MAX_TOKENS=1024
# PNS_AUTONOMY_TEMPERATURE=0.85

# ── 可选：存档根 ────────────────────────────────────────────────────────
# 默认是 /app/data/worlds，也就是 pns-data 卷里面。改它之前先读部署文档里
# 关于单写者和本地文件系统语义的那一节。
# PNS_WORLD_ROOT=/app/data/worlds
